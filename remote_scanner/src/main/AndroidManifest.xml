<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.jesse.gaea.remote.scanner">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- activity -->
        <activity android:name=".ui.activity.ScannerActivity" />

        <!-- fragment -->
        <meta-data
            android:name="atlas.fragment.intent.action.scanner.scanner"
            android:value="cn.jesse.gaea.remote.scanner.ui.fragment.ScannerFragment" />

        <!-- transactor -->
        <meta-data
            android:name="atlas.transaction.intent.action.scanner"
            android:value="cn.jesse.gaea.remote.scanner.transactor.ScannerTransactor" />

    </application>

</manifest>
